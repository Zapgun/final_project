using UnityEngine;

// Step 1: I need to store two colors that are clicked on (two variables)
// Step 2: I need to compare the two colors and see if they match
// Step 3: Print out "You win" or print out "You looser"
// Step 4: Restart the game, clear the variable so they don't contain anything in them


public class ColorMatch : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
        // Step 4: Restart the game, clear the variable so they don't contain anything in them
        DoColorMatch();
    }

    void DoColorMatch() {
        // Step 1: I need to store two colors that are clicked on (two variables)
        // Color color1, color2;
        // // Step 2: I need to compare the two colors and see if they match
        // if (color1 == color2) {
        //     // Step 3: Print out "You win" 
        //     print("You Win!");
        // } else { //or print out "You looser"
        //     print("You looser!");
        // }
    }

}
